# @package _global_
# Local launcher configuration (no SLURM)

defaults:
  - override /hydra/launcher: basic

n_gpus: 2
cpus_per_task: 16
cache_dir: null # Use stable-worldmodel default cache
wandb_entity: basileterv
wandb_project: stable-wm

wandb:
  enable: true
  entity: ${wandb_entity}
  project: ${wandb_project}

# Override trainer to use 2 GPUs explicitly
trainer:
  devices: 2  # Use exactly 2 GPUs
